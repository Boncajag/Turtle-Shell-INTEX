<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin User Maintenance</title>
</head>
<body>
  <h1>Admin User Maintenance</h1>
  <a href="/logout">Logout</a>
  <a href="/">Home</a>

  <!-- Table for displaying user records -->
  <table border="1" cellspacing="0" cellpadding="5">
    <thead>
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Phone Number</th>
        <th>Address</th>
        <th>City</th>
        <th>State</th>
        <th>Zip Code</th>
        <th>Referral Source</th>
        <th>Sewing Level</th>
        <th>Hours per Month</th>
        <th>Title</th>
        <th>Username</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% user.forEach(row => { %>
        <% if (row.title === 'User') { %>
          <tr>
            <td><%= row.first_name %></td>
            <td><%= row.last_name %></td>
            <td><%= row.email %></td>
            <td><%= row.area_code %>-<%= row.phone_number %></td>
            <td><%= row.street %></td>
            <td><%= row.city %></td>
            <td><%= row.state %></td>
            <td><%= row.zip_code %></td>
            <td><%= row.referall_source %></td>
            <td><%= row.sewing_level %></td>
            <td><%= row.hours_per_month %></td>
            <td><%= row.title %></td>
            <td><%= row.username %></td>
            <td>
              <!-- Edit Button -->
              <form action="/editUser/<%= row.volunteer_id %>" method="GET" style="display:inline;">
                <button type="submit">Edit</button>
              </form>
              <!-- Delete Button -->
              <form action="/deleteUser/<%= row.volunteer_id %>" method="POST" style="display:inline;">
                <button type="submit" onclick="return confirm('Are you sure you want to delete this user?');">Delete</button>
              </form>
            </td>
          </tr>
        <% } %>
      <% }) %>
    </tbody>
  </table>

  <!-- Button to add a new user -->
  <form action="/addUser" method="GET" style="text-align:center; margin-top:20px;">
    <button type="submit">Add New User</button>
  </form>
</body>
</html>
